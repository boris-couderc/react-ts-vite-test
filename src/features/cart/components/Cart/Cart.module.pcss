.cart {
  position: absolute;
  display: none;
  z-index: 100;
  height: 100%;
  background: #fff;
  right: 0;
  top: 0;
  min-width: 300px;
  padding: var(--spacing-m);

  &:global(.-is-open) {
    display: block;
  }
}
