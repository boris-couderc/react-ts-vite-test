.-card-box {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.details {
  flex-grow: 1;
  text-align: center;
  margin-bottom: var(--spacing-m);
}

.price {
  font-weight: bold;
  font-size: 1.25rem;
  color: var(--color-txt-dark);
}

.-list-quantifier {
  min-height: 3.5rem;
}
