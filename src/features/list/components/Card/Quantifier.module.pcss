.quantifier {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  min-height: 3.5rem;
  gap: var(--spacing-xs);

  :global(.-card-input) {
    width: 5rem;
    text-align: center;
  }
}
